// import React from 'react'
// import Picture from '../components/Picture'

// export default const VideoOrPicture(picture) = { picture.media_type == 'video' ? <AstronomyVideo picture={picture} /> : <Picture picture={picture} /> }